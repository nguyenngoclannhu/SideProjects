# Production Environment Configuration for RAG Chat System
# Copy this to .env and update values for production deployment

# Security Configuration (CRITICAL - Change these!)
SECRET_KEY=your-super-secure-secret-key-change-this-immediately-in-production
DEFAULT_USERNAME=admin
DEFAULT_PASSWORD=your-secure-password-here

# Authentication Settings
ENABLE_AUTH=true
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Cookie Security (Production Settings)
COOKIE_SECURE=true          # HTTPS only - set to true in production
COOKIE_HTTPONLY=true        # Prevent JavaScript access
COOKIE_SAMESITE=strict      # CSRF protection (strict/lax/none)
COOKIE_DOMAIN=yourdomain.com # Set your production domain

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Database Configuration (if using external services)
# QDRANT_HOST=your-qdrant-host
# QDRANT_PORT=6333
# QDRANT_COLLECTION_NAME=documents

# AI Model Configuration
# OLLAMA_HOST=http://your-ollama-host:11434
# OLLAMA_MODEL=llava
# EMBEDDING_MODEL=all-MiniLM-L6-v2

# File Upload Settings
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=200

# Production Security Checklist:
# ✅ Change SECRET_KEY to a strong random value
# ✅ Update DEFAULT_USERNAME and DEFAULT_PASSWORD
# ✅ Set COOKIE_SECURE=true for HTTPS
# ✅ Set COOKIE_SAMESITE=strict for maximum security
# ✅ Set COOKIE_DOMAIN to your production domain
# ✅ Use environment-specific values for all services
# ✅ Enable HTTPS/TLS in your reverse proxy (nginx/apache)
# ✅ Set up proper firewall rules
# ✅ Use a proper database for user management in production
# ✅ Implement rate limiting
# ✅ Set up monitoring and logging
